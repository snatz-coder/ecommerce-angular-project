import { ViewPanel } from './view-panel';

describe('ViewPanel', () => {
  it('should create an instance', () => {
    const directive = new ViewPanel();
    expect(directive).toBeTruthy();
  });
});
